# 2026-04-01

## Session — 2026-04-01 07:22 UTC

### What I did

**Research:**
- Audited Phase 0 (Feature Flags) — fully complete. Schema, lib/features.ts, admin UI, sidebar nav, page-level enforcement, seed script all exist.
- Researched SaaS feature flag patterns: Permission flags (permanent) are the right model for this portal; Kill switches for Stripe/CRM should be considered.
- Found a bug: dashboard quick action buttons (Studio, Support, Billing) were NOT gated by feature flags while the sidebar nav WAS gated.

**Build — Dashboard improvements:**
- Fixed the dashboard's Quick Actions card to filter by `enabledFeatures` — Studio/Billing/Support buttons now properly hidden when their flag is disabled
- Added time-based greeting (Good morning/afternoon/evening/night by UTC hour)
- Pulled real Plan data from `getCrmSubscription()` — shows actual plan name + status badge (Active/Past Due/Canceled/etc.) with next billing date
- Pulled real Site Status from `getCrmSite()` — shows CRM-reported status instead of hardcoded "Active"
- Graceful degradation: "No plan" / "Unknown" / empty state when CRM data unavailable
- Hidden "View all tickets" link when support flag is disabled
- Added 21 unit tests for the new pure logic functions (greeting, quick-action filtering, plan display, site status)

### Tests
- 159 tests passing (was 138 before this session)

### Commit
- `3833d98` — feat(dashboard): feature-flag-gate quick actions, real CRM data, time greeting

### Next steps (high value)
1. Add `stripe_enabled` and `crm_integration_enabled` kill-switch flags to the registry
2. TV Feed placeholder → real management UI (Phase 5/6)
3. Billing page needs Stripe Customer Portal session redirect — currently just a placeholder
4. Studio page needs Sanity config wired up per tenant (tenant has `sanityProjectId` in CRM)

---

## Session — 2026-04-01 08:00 UTC

### What I did

**Research:**
- Audited entire Phase 0 Feature Flag system end-to-end: all components complete (171 tests pass, up from 159)
- Found `sonner` v2 was in `package.json` but `Toaster` was never rendered anywhere — gap in UX feedback
- Researched NextAuth JWT staleness: the JWT-embedded `enabledFeatures` pattern is correct for this portal (60s TTL cache, flag changes rare). Architecture is pragmatic and sound.
- Identified the exact UX gap: `FeatureFlagToggle` silently succeeded/failed with no user feedback

**Build — Sonner toast feedback for FeatureFlagToggle:**
- Added `<Toaster position="bottom-right" richColors closeButton />` to `app/layout.tsx`
- Rewrote `FeatureFlagToggle.tsx` with `useEffect` + `prevStateRef` pattern to fire toast on state transition (not on every re-render)
  - `toast.success(\`${flag.label} enabled\`)` on success transition
  - `toast.error(state.error)` on error transition
- Wrote 12 new unit tests covering the toggle state machine and toast trigger logic

### Tests
- 171 tests passing (was 159 before this session)

### Commit
- `8abb4d3` — feat(admin): add Sonner toasts for feature flag toggle feedback

### Still open (next sessions)
1. TV Feed placeholder → real management UI (Phase 5/6)
2. `stripe_enabled` / `crm_integration_enabled` kill-switch flags
3. Billing page needs Stripe Customer Portal redirect
4. Studio page needs Sanity config per tenant
5. Agency admin: `/admin/clients/new` — create new tenant form
