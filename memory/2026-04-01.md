# 2026-04-01 — Session Log

## Email fix (ctwebsiteco repos)
- Scanned ALL 29 ctwebsiteco repos for wrong author email (`openclaw@openclaw.ai`, `noreply@anthropic.com`)
- Found and fixed: `crm-frontend` (301 commits), `client-portal` (410 commits), `v0-apizza-grande-website` (PR #5), `v0-southington-gardens-clone` (PR #6)
- All others (26 repos) were already clean
- All rewrites force-pushed to GitHub
- Global git identity: `Sean Conway <srconway0@gmail.com>`

## Fairway Irrigation fix
- Vercel deployment was failing — root cause: `@next/third-parties` requires Next.js `^13-15`, incompatible with v16
- Fix: removed `@next/third-parties`, replaced with vanilla gtag.js script injection
- Added `vercel.json` with explicit `pnpm install` / `pnpm build`
- CLI deployment succeeded: `v0-fairway-irrigation-website-43tuhki1e.vercel.app` ✅ READY

## CRM tickets — confirmed working
- Ticket stages ARE moving in DB correctly (16 Backlog, 2 To Do, 2 Resolved, etc.)
- Dashboard board view reflects live DB data
- `X-API-Key` header is correct auth method for CRM MCP endpoint
- Ticket board drag-and-drop works (tested via API)

## client-portal (2026-04-01 work)

### Phase 0 complete today:
- Middleware feature flag enforcement: JWT embeds `enabledFeatures` on sign-in, middleware decodes on Edge Runtime and redirects disabled routes
- `lib/auth.ts`: added `enabledFeatures` to JWT token + Session type
- `middleware.ts`: reads `enabledFeatures` from JWT, blocks `/studio`, `/support`, `/billing`, `/content/tv-feed`, `/content/media` if flag disabled
- `db:seed`: seeded 6 feature flags (studio, support, billing, content_hub, tv_feed, media_library)
- NOTE: `DATABASE_URL` env var overrides `.env` file with PostgreSQL — use `DATABASE_URL="file:./prisma/dev.db"` prefix for local Prisma commands

### Phase 1 still needed:
- `/studio` page — embedded Sanity Studio (missing)
- `/support` list page — missing
- `/support/[id]` detail page — missing (only `new` is built)
- `/content` placeholder page — already exists

### Tests: 121 passing (13 test files)

### Vercel: client-portal deployed at `v0-client-portal-*.vercel.app` (check via Vercel dashboard)

## CRM API
- Key: `crm_73c6c876ef61204de0776e30c8f76afd9a157d22a3209a2bd991ef60af6abb53`
- Auth: `X-API-Key` header (NOT `Authorization: Bearer`)
- Endpoint: MCP at `https://crm-api-1016182607730.us-east1.run.app/mcp` (JSON-RPC)
- Townsend ticket: status=in_progress (updated via MCP)

---

## 05:24 UTC — Session Run

### What I did

**Phase 6: Built the `/settings` page** — explicitly linked in the sidebar, previously missing.

1. **Bug fix in `lib/auth.ts`:** `session.user.id` was never being propagated from the JWT to the session. Fixed by adding `token.id = user.id` in the `jwt` callback and `session.user.id = token.id as string` in the `session` callback. This was blocking the settings page from knowing which user to update.

2. **Created `app/(portal)/settings/actions.ts`:** Two server actions:
   - `updateProfileAction(prevState, formData)` — validates name + email with Zod, checks email uniqueness within tenant, updates `PortalUser` in Prisma. Requires auth.
   - `updatePasswordAction(prevState, formData)` — verifies current password with `bcrypt.compare`, hashes new password with `bcrypt.hash(password, 12)`, persists to Prisma. Requires auth.

3. **Created `app/(portal)/settings/settings-form.tsx`:** Client Component using `useActionState` for both forms (profile + password). Shows inline field errors, success messages, and uses existing UI components (`Card`, `Input`, `Label`, `Button`, `Separator`).

4. **Created `app/(portal)/settings/page.tsx`:** Server Component — fetches current user from Prisma, renders `SettingsForm`. Requires auth (redirects to `/login` if not authenticated).

5. **Created `components/ui/separator.tsx`:** Simple separator component (was missing from the UI library).

6. **Wrote `__tests__/unit/app/(portal)/settings/page.test.ts`:** 17 tests covering all validation paths for profile and password actions (empty name, invalid email, wrong current password, password too short, mismatch, etc.). All 138 tests pass.

7. **TypeScript: clean** — `tsc --noEmit` returns no errors.

### Files changed
- `lib/auth.ts` — added `session.user.id` propagation
- `app/(portal)/settings/page.tsx` — new
- `app/(portal)/settings/settings-form.tsx` — new
- `app/(portal)/settings/actions.ts` — new
- `components/ui/separator.tsx` — new
- `__tests__/unit/app/(portal)/settings/page.test.ts` — new

### Research notes (for future work)
- Phase 0 (Feature Flags): essentially complete per research log
- Phase 6 remaining: full tenant theme (header, footer), agency "New Client" onboarding page
- The portal is feature-complete for a v1: auth, support tickets, billing, content hub, Sanity Studio, feature flags, settings

