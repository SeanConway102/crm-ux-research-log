# 2026-03-29

## Heartbeat Summary (2026-03-29)

- **Browser**: Chrome .deb installed (previously Snap chromium had AppArmor issues). Playwright installed. CDP remote debugging on port 9222. Visual browser confirmed working with CRM login screenshot.
- **Lighthouse**: CLI installed and working. Scored v0-crm-frontend: Performance 36 (LCP 10s, TBT 6.6s - needs work), Accessibility 95, Best Practices 100, SEO 100.
- **CRM**: Healthy, 2h uptime, no issues.
- **Frontend**: Returns 307 (redirect to login - expected behavior).
- **Notes**: gh CLI not available in this container. Git repos not in expected paths.
# 2026-03-29

## Heartbeat Summary (2026-03-29)

- **Browser**: Chrome .deb installed (previously Snap chromium had AppArmor issues). Playwright installed. CDP remote debugging on port 9222. Visual browser confirmed working with CRM login screenshot.
- **Lighthouse**: CLI installed and working. Scored v0-crm-frontend: Performance 36 (LCP 10s, TBT 6.6s - needs work), Accessibility 95, Best Practices 100, SEO 100.
- **CRM**: Healthy, 2h uptime, no issues.
- **Frontend**: Returns 307 (redirect to login - expected behavior).
- **Notes**: gh CLI not available in this container. Git repos not in expected paths.

## Session Progress — Deployed & Shipped

### Announcement Banner + Changelog
- Created `AnnouncementBanner` component with CSS animation + `CHANGELOG.md`
- Pushed to Vercel, live on CRM frontend
- **Blocked**: `NEXT_PUBLIC_ANNOUNCEMENTS` env var still needs to be set in Vercel dashboard

### Bugs Fixed This Session

**Issue #25 (FIXED):** RegisterSW duplicate toast spam — `navigator.serviceWorker.addEventListener` was registering duplicate listeners on re-renders. Fixed by moving listener registration into `useEffect` with proper cleanup.

**Issue #27 (FIXED):** Deals "Load More" pagination — was replacing data instead of appending. Fixed by checking `cursorIdx > 0` before setting data.

**Issue #28 (FIXED):** Pagination bug across 8 pages — Same `setData(res.data || [])` pattern was overwriting instead of appending on all paginated list pages (activities, companies, payments, tickets, products, invoices, subscriptions). Fixed proactively across all 8 pages. Issue filed.

**Issue #30 (FIXED):** Silent `catch {}` swallowing errors — knowledge-base, wiki, contacts, tickets pages all had bare catch blocks. Replaced with `toast.error()` to surface failures to users. Issue filed.

### Cron Job
- `/root/.openclaw/workspace-agency/crm-cron.sh` running at :30 past every hour
- Scans: console.error count, TODO/FIXME, TypeScript `any` count, GitHub issue checks
- Fixed jq bug: `// 0` default for empty GitHub API results
- Cron at 18:30 ran clean — all checks passed

### Known CRM Issues (Not Fixed)
- **Sites page stale scores**: CRM shows Lighthouse scores that don't match fresh audits (salisburykitchen shows 87 in CRM, actual is 58)
- **Client site scores**: Brimatco (27), Fairway (27), Energy Busters (38) — all much worse than CRM stored values
- **Issue #10**: findKBPath tool still open — Knowledge Base "Open in CRM" link broken
- **Issue #26**: TODO/FIXME audit — 4 TODOs, 9 `any` types found; decide whether to fix or suppress

### Pattern: Common Bug in CRM Frontend
All list pages with "Load More" have TWO bugs:
1. **Pagination overwrite**: `setData(res.data || [])` replaces instead of appends when cursorIdx > 0
2. **Silent failures**: bare `catch {}` swallows errors without user feedback

### GitHub Issues Filed
- #10: findKBPath tool (KB "Open in CRM" broken)
- #25: RegisterSW duplicate toast (FIXED)
- #26: TODO/FIXME/HACK audit
- #27: Deals pagination load-more (FIXED)
- #28: Pagination pattern across 8 pages (FIXED)
- #30: Silent catch {} swallows errors (FIXED)

## QA Failure — 20:26 UTC

**What happened:** Applied mass `sed` replacement for cursorIdx pagination without reading each file first. The tickets page uses a flat 200-limit fetch (no cursorIdx), so applying `cursorIdx < 0` pattern caused a runtime error. Pushed to production without browser testing.

**What should have happened:**
1. Read the tickets page code first
2. Understood it uses flat fetch, not cursorIdx
3. Written a targeted fix
4. Run Playwright smoke test before pushing
5. Reviewed the diff

**Files verified correct after audit:**
- payments, products, invoices, subscriptions: cursorIdx pattern CORRECT
- companies: page-based pattern CORRECT  
- deals (closed), sites (closed), tickets (closed): page-based pattern CORRECT
- tickets (open): flat fetch, correct

**QA process now enforced:** QA.md created with pagination pattern reference and smoke test script at `~/repos/crm-frontend/scripts/smoke-test-pagination.js`
